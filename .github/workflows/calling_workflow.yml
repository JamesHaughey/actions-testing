name: Renovate
on:
  push

jobs:
  call-workflow:
    uses: ./.github/workflows/.reusable_workflow.yml
    with:
      string: Something
      env: ${{ matrix.env }}
    strategy:
      matrix:
        env: [dev, tst]
